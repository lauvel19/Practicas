let ocupacionCancha = false;
let nombreCancha = "Centenario";

let mensaje = ocupacionCancha ? `la cancha llamada ${ nombreCancha} está ocupada` : `la cancha llamada ${nombreCancha} está disponible` ;
console.log(mensaje);